name: Trigger auto deployment for ca-api-cnxlpyxkcdi5g

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ca-api-cnxlpyxkcdi5g-AutoDeployTrigger-63e63d95-1a70-4b04-aa6e-4cf9b254b1b6.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CAAPICNXLPYXKCDI5G_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CAAPICNXLPYXKCDI5G_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CAAPICNXLPYXKCDI5G_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: crcnxlpyxkcdi5g.azurecr.io
          registryUsername: ${{ secrets.CAAPICNXLPYXKCDI5G_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAAPICNXLPYXKCDI5G_REGISTRY_PASSWORD }}
          containerAppName: ca-api-cnxlpyxkcdi5g
          resourceGroup: rg-faq-agent-env
          imageToBuild: crcnxlpyxkcdi5g.azurecr.io/ca-api-cnxlpyxkcdi5g:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


